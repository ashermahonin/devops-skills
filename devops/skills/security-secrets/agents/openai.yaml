interface:
  display_name: "Security And Secrets"
  short_description: "Secrets, IAM, RBAC, scanning, signing, and audit."
  brand_color: "#B3261E"
  default_prompt: "Use $security-secrets to design or review this access and secret flow with least privilege, audit, rotation, and validation."
policy:
  allow_implicit_invocation: true
